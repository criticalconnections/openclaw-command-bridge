# Command Bridge Configuration

# Server port
PORT=3333

# OpenClaw Gateway Configuration
# The dashboard auto-detects these from ~/.openclaw/gateway.yaml
# Override them here if needed
OPENCLAW_GATEWAY_URL=http://localhost:18789
OPENCLAW_GATEWAY_TOKEN=your-gateway-token-here

# OpenClaw Workspace (read-only mount for Docker)
OPENCLAW_WORKSPACE=~/.openclaw/workspace

# Personalization
USER_NAME=User
TIMEZONE=America/New_York
WEATHER_LOCATION=auto

# Node environment
NODE_ENV=production
